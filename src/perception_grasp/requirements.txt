# Perception Grasp 运行时依赖
# PyTorch/cu128 is installed separately in Dockerfile.grasp so the rest of the
# Python stack can still resolve from the normal PyPI index.

open3d==0.17.0
numpy==1.23.5
scipy==1.10.1
PyYAML==6.0.1
empy==3.3.4
opencv-python-headless==4.10.0.84
ninja==1.11.1.1

# GraspNet 依赖（需要手动克隆仓库）
# git clone https://github.com/graspnet/graspnet-baseline.git
# cd graspnet-baseline && python -m pip install -e .

# ROS Python 依赖
rospkg
catkin_pkg
